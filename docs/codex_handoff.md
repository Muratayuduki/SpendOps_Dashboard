# Codex 作業引き継ぎ

このファイルは、Codex が次回以降の作業開始時に確認するための運用メモです。
README にはプロジェクト概要と仕様を置き、Codex 向けの進め方、更新履歴、次回作業、未決事項はこのファイルで管理します。

## 作業開始時の必須手順

1. `README.md` でプロジェクト概要、最新仕様、完了条件を確認する
2. `docs/system_prompt_guardrails_v2.md` でガードレール、禁止事項、出力方針を確認する
3. この `docs/codex_handoff.md` で進捗、Notion更新履歴、未決事項、次回作業を確認する
4. 既存資料、既存コード、既存draw.ioを確認してから変更する
5. 不明点が成果物の品質に大きく影響する場合は、推測せず質問する
6. 個人情報、金融情報、メール本文全文、カード番号、ログイン情報、認証コードを保存・出力しない
7. 破壊的変更、外部公開、削除、本番デプロイ、秘密情報の変更は事前確認する

## 最新仕様の要点

- 個人用途の家計簿・資産管理ダッシュボードから、複数人のCSVを収集して分析する比較分析サービスへ方向転換する
- 各ユーザーがCSVをアップロードし、システムが正規化、月次集計、匿名化集計を行う
- 各ユーザーのデータを集計し、MVPでは月間支出、カテゴリ別支出、支払い方法別支出、取引件数、前月比、全体平均との差を比較できるようにする
- 中央値と分位はMVP対象外とし、利用者数が増えてから追加する
- 比較分析は、原則として匿名化・集計値ベースで行い、生データの横並び表示は避ける
- 初期版ではPayPay、JCB、三井住友VISAの3種類のCSV分析を行う
- MyJCBとSmartHRはローカルChromeのログイン済みセッションから自動取得できることを確認済みだが、多人数版MVPではCSVアップロードを基本にする
- Cognitoで一般ユーザーと管理者を分ける
- 一般ユーザーは本人データのみ、管理者は全体集計・取込状況・エラー原因ファイルの特定のみ行う
- 管理者も個人情報、生明細、CSV原本は閲覧できない
- 原本CSVと個別明細は保存せず、DynamoDBには月別集計と取込記録だけを保存する
- ユーザー登録時に、収支データを匿名化・集計して比較分析へ利用する同意を取る
- 退会時は本人に紐づく月別集計、取込記録、ユーザー情報を削除し、再識別できない全体集計のみ残す
- CSV取込は部分取込を許可し、該当行以外を取り込んだうえで失敗行番号と理由を表示する
- 氏名、支出先、摘要、商品名、個別明細はDynamoDBへ保存しない
- 比較対象は同じデータソース同士から開始し、最終的には全支出を統合した比較分析を目指す
- 取引明細はブラウザ内だけで日付単位に扱い、保存・分析復元・他者比較は月別集計を中心にする
- 収入・支出・資産推移を、個人内推移と集団平均との差の両方で見られるようにする
- AWS Cost ExplorerによるAWS料金取得は初期版から削除する。複数人の収支CSV比較分析に集中する
- AWS基盤はTerraformで管理する。2026-07-15に全リソースを削除済みで、再構築・変更・デプロイもPlan確認とユーザー承認後に行う

## 現在の停止位置（2026-07-15 AWS削除後）

- 公開サイトとAPIは停止・削除済み
- Terraform Apply結果は0件追加、0件変更、38件削除
- Terraform stateは0件。DynamoDB、Cognito、S3、Lambda、API Gateway、CloudFront、CloudWatch Logs、IAMもすべて0件を確認済み
- 1画面UI、PayPay／JCB／VISA分析、Cognito認証、DynamoDB月別集計保存、実ユーザー匿名比較、全支払い統合、詳細分析コメント、完全な円表記、手動カテゴリ編集のコードはローカルに保持
- フロントエンド29件、Lambda 16件の合計45テスト成功
- 原本CSV、商品名、利用店名、個別明細はAWSへ保存していない
- `docs/0715log.md` の内容はREADMEとこのファイルへ統合し、個別ログは削除する
- 7月31日完成を目標にNotionガントを再作成済み。新DBは30件（完了9、進行中0、未着手20、保留1）
- Destroy Planは追加0、変更0、削除38。DynamoDBは月別集計84件、取込履歴21件、比較集計0件、取引明細0件、Cognitoは3アカウント
- 削除前のDynamoDB 4テーブルはPITRが35日で有効だったため、削除時システムバックアップから35日以内に新テーブルへ復元可能。Cognitoは自己復元不可で、削除後14日以内のAWS Supportによる復旧支援のみ

## 更新履歴

- 2026-07-03: Notionの「SpendOps Dashboard 開発計画」をPayPay CSV化方針に更新。
- 2026-07-08: 横浜銀行もGmail取得対象外に変更。初期版ではJCBカードも含め、PayPay / 横浜銀行 / JCBカードをCSVアップロード方式に統一する方針へ更新。
- 2026-07-08: Notion本ページの親ページ、01〜06ページ、項目別ガントチャート、主要タスクDB項目をCSV統一方針へ更新。
- 2026-07-08: Notionに「07 類似サービス・差別化・懸念点」を追加。類似サービス、SpendOpsの差別化、セキュリティ面・システム面の懸念点を整理。
- 2026-07-08: draw.io構成図を開けない問題に対応。特殊AWSアイコンスタイルを通常図形へ置換し、diagrams.net標準のmxfile属性へ更新。
- 2026-07-08: SmartHR給与明細の自動取得を検証。ローカルChromeのログイン済みセッションから2026年1月〜6月分のPDFを取得し、支給額を抽出できることを確認。
- 2026-07-08: MyJCBの自動取得を検証。ローカルChromeのログイン済みセッションから明細CSVを取得し、既存JCB CSV形式と同じ内容で取り込めることを確認。
- 2026-07-08: 個人用途から複数ユーザー比較分析へ方向転換する前提を追加。匿名化・集計値中心での比較分析を重視する。
- 2026-07-08: 要件を「複数人からCSVを受け取り、匿名化・正規化・月次集計・比較分析を行うサービス」へ更新。Cognitoによる一般ユーザー/管理者のログイン分離、原本CSV/正規化データ/個人集計/比較集計の分離を前提に追加。
- 2026-07-08: AWS Cost ExplorerによるAWS料金取得を初期版対象外に変更。利用者数が少なく比較分析の価値に直結しないため、複数人CSV分析へ集中する。
- 2026-07-08: CSVフォルダ内にPayPay、JCB、三井住友VISAの3種類のCSVがあることを確認。初期版では3種類のCSV分析を対象にする。PayPayはUTF-8ヘッダーあり、JCB/VISAはShift_JISでメタ情報行の読み飛ばしが必要。
- 2026-07-08: 管理者も個人情報・生明細を閲覧できない設計、原本CSV非保存、登録時同意、退会時の本人紐づきデータ削除、部分取込方針を確定。
- 2026-07-15: Notionに「SpendOps Dashboard 7月完成ガント」を新設。7/15までの完了実績と7/31までの4スプリントを30件のタスクへ再編し、タイムライン・残作業ボード・全件一覧を作成。
- 2026-07-15: 旧ガントと旧タスクDBは削除せず履歴として更新停止にし、親ページ・スケジュールページ・項目別ガントを7月完成方針へ更新。
- 2026-07-15: Terraform Destroy Planで38リソース削除、DynamoDBのPITR 35日、Cognitoの自己復元不可と14日Support支援経路、削除対象件数を確認。
- 2026-07-15: ユーザー最終承認後にDestroy Planを適用。0追加、0変更、38削除で完了し、Terraform stateとAWS主要8分類がすべて0件であることを確認。

## 2026-07-08 要件整理履歴

2026-07-08時点で、サービス方針を個人資産管理から複数人CSV比較分析サービスへ切り替えた。

今回確定した内容:

- 初期版はPayPay、JCB、三井住友VISAの3種類のCSV分析を行う
- PayPay CSVはUTF-8で、1行目にヘッダーがある
- PayPay CSVでは、取引日、出金金額、入金金額、取引内容、取引先、取引方法、支払い区分、利用者、取引番号を確認済み
- JCB CSVはShift_JISで、先頭に支払日・支払金額合計などのメタ情報があり、明細ヘッダー行まで読み飛ばす必要がある
- JCB CSVでは、ご利用者、カテゴリ、ご利用日、ご利用先など、ご利用金額、支払区分、お支払い金額、摘要、備考を確認済み
- 三井住友VISA CSVはShift_JISで、先頭にカード情報系のメタ行があり、明細行は固定列として扱う必要がある
- 管理者も個人情報、生明細、CSV原本を閲覧できない
- 管理者が確認できるのは、全体集計、取込状況、エラー件数、エラー原因ファイル、失敗行番号、失敗理由に限定する
- 原本CSVは保存しない。取込時に読み取り、加工後の正規化済みデータだけ保存する
- 氏名は削除する
- 支出先、摘要、取引内容はカテゴリ分類に必要なため保存する
- ユーザー登録時に、匿名化・集計して比較分析へ利用する同意を取る
- 退会時は本人に紐づく月別集計、取込記録、ユーザー情報を削除する
- 退会後も、個人を再識別できない全体集計だけ残す
- CSV取込は部分取込を許可する
- 取込に失敗した行は除外し、それ以外の行で集計する
- 画面には「何行目のデータ取得に失敗したため、それ以外で集計を行います」という趣旨のエラーを表示する
- MVPでは中央値と分位は表示しない
- MVPの分析項目は、月間支出総額、月間収入総額、カテゴリ別支出、支払い方法別支出、取引件数、前月比、全体平均との差とする
- 初期の比較対象は同じデータソース同士にする
- 最終的にはPayPay、JCB、VISAなどを統合した全支出比較を目指す
- AWS Cost ExplorerによるAWS料金取得は初期版から削除し、AWSは認証、保存、集計、監査、暗号化の実行基盤として使う

今回更新した資料:

- `README.md`
- `docs/system_prompt_guardrails_v2.md`
- `docs/codex_handoff.md`
- `notion/spendops_dashboard_notion_plan_with_gantt.md`
- `drowio/spendops_aws_architecture.drawio`
- Notion本体の `08 要件変更：複数人CSV比較分析サービス`

## Notion更新履歴

2026-07-03に、Notionの「SpendOps Dashboard 開発計画」を更新済みです。
2026-07-08に、ローカル資料とNotion本ページをCSV統一方針へ更新しました。
2026-07-15に、7月31日完成を目標とする新ガントDBと3つのビューを作成し、AWS削除後の公開E2E保留条件まで反映しました。

親ページ:

- SpendOps Dashboard 開発計画
- URL: https://app.notion.com/p/391e666f35db81fa9c1aef83769c6f3a

更新済みページ:

| ページ | URL | 内容 |
|---|---|---|
| 04 課題・次アクション | https://app.notion.com/p/391e666f35db81598841f835feea473e | PayPay CSV化、全自動性低下への改善案、次アクションを更新 |
| 05 企画書 | https://app.notion.com/p/392e666f35db81449c61edf814034178 | 新規作成。企画概要、背景、課題、提供価値、成功条件を記載 |
| 06 要件定義 | https://app.notion.com/p/392e666f35db81e28aadfb321c385d9d | 新規作成。機能要件、非機能要件、データ項目、API、画面要件を記載 |
| 07 類似サービス・差別化・懸念点 | https://app.notion.com/p/397e666f35db8149aa8bf7e60b4cc7ad | 新規作成。類似サービス、差別化、セキュリティ面・システム面の懸念点を記載 |
| 08 要件変更：複数人CSV比較分析サービス | https://app.notion.com/p/397e666f35db815abfe8de869eaf340c | 新規作成。複数人CSV収集、匿名化集計、Cognito権限分離、AWS利用理由、MVP集計項目を記載 |
| 03 スケジュール・タスク管理 | https://app.notion.com/p/391e666f35db811bbd62ea26aab16804 | 7/15までの実績と7/16〜7/31の4スプリントへ更新 |
| 項目別ガントチャート | https://app.notion.com/p/391e666f35db81b69989d5afda780d04 | 7月マイルストーン、進捗数、運用ルール、新ガントへの導線を反映 |
| SpendOps Dashboard 7月完成ガント | https://app.notion.com/p/6aaf54a80b0e4415b0c2b6b092a4c55d | 30タスク、タイムライン、残作業ボード、全件一覧を新規作成 |

2026-07-08追加更新:

- 親ページに最新方針を追記
- 01 概要・目的をCSV統一方針へ更新
- 02 技術設計をCSV取込構成へ更新
- 03 スケジュール・タスク管理をCSV取込中心へ更新
- 04 課題・次アクションをPayPay / 横浜銀行 / JCB CSV統一方針へ更新
- 05 企画書をCSV統一方針へ更新
- 06 要件定義をCSV統一方針へ更新
- 07 類似サービス・差別化・懸念点を追加
- SmartHR給与明細のローカル自動取得を検証
- MyJCB明細CSVのローカル自動取得を検証
- 個人用途から複数ユーザー比較分析へ方向転換する前提を追加
- Notionに「08 要件変更：複数人CSV比較分析サービス」を追加し、親ページにも最新方針への導線を追記
- 項目別ガントチャートの説明文をCSV取込フェーズへ更新
- タスク管理DBの主要な旧Gmail/GASタスク名をCSV取込タスクへ更新

## ローカル資料の状態

| パス | 内容 | 注意 |
|---|---|---|
| `notion/spendops_dashboard_notion_plan_with_gantt.md` | Notion計画資料のローカル版 | 2026-07-15に7月完成ガントと同期 |
| `drowio/spendops_aws_architecture.drawio` | AWS構成図 | 2026-07-08にCSV統一方針へ更新。開けない問題に対応するため通常図形ベースへ修正 |
| `docs/system_prompt_guardrails_v2.md` | system prompt・ガードレール | 2026-07-08にCSV統一方針へ更新 |
| `README.md` | プロジェクト概要、公開状態、2026-07-15実装履歴、次回作業 | 2026-07-15の作業ログを統合済み |
| `AGENTS.md` | Codexのリポジトリ作業ルール | 作業開始時に読むファイルを定義 |

## 次回以降の優先作業

1. AWS削除後を前提に、新規登録、メール確認、ログイン、CSV取込、DB保存、再読込後の復元についてE2Eチェックリストとローカル回帰テストを整備する。公開E2EはAWS再構築を別途承認した場合のみ実施する
2. 手動カテゴリを次回CSVでも再利用できるようにする。利用店名をDBへ保存しない前提で、端末内保存または不可逆化したルールキーの設計を先に確定する
3. PayPayチャージとカード明細の二重計上候補を検出し、利用者が確認・除外できるようにする
4. PayPay、JCB、VISAの返金・取消・速報／確定行を確認し、相殺または対象外ルールを実装する
5. 支払い元ごとの最終取込日と未取込警告を画面へ追加する
6. 横浜銀行CSVの形式を確認し、支出ではなく入出金履歴として別枠実装する
7. 実比較ユーザー数を増やして匿名比較の精度を確認し、合成参考値への依存を減らす
8. 退会画面、本人データ削除、保持期間、再構築・バックアップ方針を確定する

次回はまず1を行う。AWSを再構築して実メール確認や本番データ投入を行う場合も、認証コード、CSV内容、個人の支出額はチャットやログへ出力せず、ユーザー自身の画面操作で確認する。

## 未決事項

- PayPay CSVで返金・取消がどのように表現されるか
- 横浜銀行 CSVの実際のカラム名
- 横浜銀行 CSVの文字コード
- 横浜銀行 CSVで入金、出金、振替がどのように表現されるか
- JCB CSVで速報、確定、取消がどのように表現されるか
- 三井住友VISA CSVの固定列の意味を正式に定義する
- 三井住友VISA CSVで取消・返金がどのように表現されるか
- SmartHR給与明細の取得対象を支給額合計だけにするか、別項目も保存するか
- SmartHR給与明細の保存先をDynamoDBのみとするか、PDF一時保存を許可するか
- MyJCBのCSV取得を毎月自動実行するタイミングを20日以降固定にするか、月末にするか
- ローカルChrome自動取得ツールをWindows常駐で走らせるか、手動起動にするか
- 複数ユーザー比較分析で、匿名化の粒度をどこまで上げるか
- 統合支出分析で、PayPay / JCB / VISA の重複支出をどう判定するか
- 手動カテゴリルールを、利用店名を保存せずにどこへ永続化するか
- 保存済み月別集計だけで明細を復元できないことを、UI上でどう説明するか
- 他者比較に使う指標を支出総額、カテゴリ比率、収入比率のどこまで広げるか
- 比較対象の最小人数は現在5人。利用者数増加後により高い匿名化基準へ変更するか
- AWS Cost Explorer APIは初期版では実装しない。将来も必要性が出るまでは対象外

## 2026-07-15 公開プロトタイプ更新

- AWS基盤はTerraformで作成済み。公開URLは `https://dneqhtcxytwrd.cloudfront.net`
- デスクトップ画面は縦スクロールなしの1画面構成へ変更済み
- CSVは複数同時選択に対応し、PayPayとクレジットカードを別タブで分析する
- 他者比較がデフォルト。個人比較は切替ボタンで選択する
- 比較用ダミーはPayPayとカードを分離し、それぞれ年代5区分×性別2区分×12人の合成120人コホートとした
- 元プロファイルはPayPay 1人相当、カード2人相当であり、実統計ではない。画面でも参考値として明示する
- カテゴリ別と支払い方法別を切替表示できる
- 分析コメントを主要領域、支払い方法を小さい補助領域へ配置した
- 支払い明細ダイアログで日付、取引先／利用店、カテゴリ、支払い方法、金額を端末内表示する
- 商品名・品名列が存在するCSVでは商品名を表示する。PayPay、JCB、VISAはCSVに含まれる取引先・利用店を表示する
- 明細と実CSVはAWSへ送信しない。公開比較ファイルは支払い方法別人数、月平均、カテゴリ平均だけを含む
- PayPayの月途中エクスポートは先頭月・末尾月を暫定扱いにし、最新完全月を初期表示する
- ローカルテスト14件成功。公開4資産はHTTP 200。最終Terraform再Planは差分なし
- アプリ内ブラウザが利用できなかったため、自動スクリーンショット確認は未実施

## 2026-07-15 認証・DB保存・実ユーザー比較の公開

- Cognitoのメールアドレス＋パスワード認証、メール確認コード、ログアウトを実装・公開済み
- トークンは `sessionStorage` へ保持し、パスワードと確認コードは保存しない
- 原本CSVと明細はブラウザ内で解析し、DynamoDBにはPayPay/カードの月別集計だけを保存する
- `user_monthly_summaries`: 月、支払い種別、月間支出、件数、固定カテゴリ別・支払い方法別集計、暫定月、更新時刻
- `import_batches`: 取込ID、対象月、支払い種別、集計件数、検証件数、同意バージョン
- 店舗名、商品名、ファイル名、個別取引、メールアドレスはDynamoDBへ保存しない
- 他者比較は本人と暫定月を除外し、他5人以上の場合だけ匿名平均を表示する。5人未満は実平均を返さず、合成比較へフォールバックする
- ログイン後は保存済み月別集計を再取得でき、月別推移と本人内比較を表示できる
- 保護API: `POST /imports`、`GET /reports`、`GET /reports/{month}`
- フロンエンド19件、Lambda 13件の合計32テスト成功
- Terraform Apply: 2件追加、6件更新、0件削除。CloudFrontキャッシュ無効化完了
- 公開資産4点とヘルスAPIはHTTP 200、`GET /reports` は未認証でHTTP 401を確認

AWS削除前に予定していた確認（再構築を承認した場合のみ実施）:

1. 公開サイトでユーザー自身が新規登録し、確認メールを完了する
2. ログイン後にPayPay/カードCSVをアップロードし、画面の「DB保存済み」表示を確認する
3. 再読込み後に「保存済み分析」から月別分析を復元できることを確認する

## 2026-07-15 全支払い累計の公開

- PayPayとカードの両方があるとき、「全支払い」を初期表示する
- 同じ月のPayPay・JCB・VISAを合算し、支出総額、件数、カテゴリ、支払い方法別割合、月別推移を表示する
- 保存済みPayPay/カード集計は、フロンエンドで同じ月ごとに統合する。既存DBデータの移行は不要
- 統合月のどちらかが未取込、または暫定の場合は「一部期間」とする
- `GET /reports/{month}?source=ALL` は本人のPayPay/カード集計を統合し、他ユーザーの両支払い完全月が5人以上の場合だけ匿名平均を返す
- PayPayチャージがカード明細にもある場合の二重計上は自動解消せず、注意文を表示する
- フロンエンド24件、Lambda 16件の合計40テスト成功
- Terraform Apply: 0件追加、3件更新、0件削除。DB・Cognito・保存済みデータは無変更
- CloudFrontキャッシュ無効化と公開HTTP確認は完了

## 2026-07-15 円表記・分析コメント・カテゴリ編集（公開済み）

- 円グラフ中央の支出額を省略しない円表記へ変更した
- 分析コメントを、総額・件数、比較差額、前月差、カテゴリ／支払い方法の偏り、具体的な確認先を含む内容へ変更した
- CSVカテゴリの表記ゆれを固定カテゴリへ正規化し、店舗名による自動分類ルールを拡充した
- 支払い明細のプルダウンからカテゴリを変更し、同じ支払い方法・同じ利用店へ一括反映できる
- ログイン中の変更は修正後の月別集計だけDynamoDBへ再保存する。利用店名や明細は保存しない
- 手動分類ルールの永続保存は未実装。現在のCSV分析内だけに適用する
- フロントエンド29件、Lambda 16件の合計45テスト成功
- Terraform Apply: 0件追加、3件更新、0件削除。DB、Cognito、Lambda、APIは無変更
- CloudFrontキャッシュ無効化完了。公開HTML／JavaScript／CSSの更新とヘルスAPIの `ok` を確認した
