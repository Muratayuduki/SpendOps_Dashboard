# 日報 2026-09-02

## 今日行ったこと

- 新しいAWSアカウントへTerraform第1段階を再構築し、CloudFront既定ドメイン、API、Cognito、Lambda、DynamoDBの稼働と再Plan差分0を確認した。
- ログイン中メールアドレスのマスク表示、ログイン時のお試し導線非表示、CSV読込表示と比較文言の整理を行い、フロント47件・Lambda 24件、合計71件のテスト成功を確認した。
- Cognito未確認ユーザーと月別比較の挙動を調査し、現在のTerraform実装と稼働状態に合わせてAWS構成図をDraw.io、仕様sidecar、SVG、PNGで更新した。

## 残っている課題

- Cognitoの確認コード入力を閉じた後、`UNCONFIRMED`ユーザーが確認画面へ戻り、コードを再送できる導線が未実装である。
- Cognitoユーザーによるログイン、保存、再取得の公開E2Eは未確認である。
- ACM証明書はDNS検証待ちで、独自ドメイン、CloudFront alias、Cloudflare DNSは未有効である。
- ソース別最終取込日と未取込警告、自動分類と表記ゆれ対応には追加実装と実例検証が必要である。

## 今後の開発工程

1. 未確認ユーザーの確認コード再送・入力再開フローを実装し、新規登録からログインまでを一連で検証する。
2. 月別比較と本人別保存の公開E2Eを複数のテストアカウントで確認し、表示とAPI結果の不一致を切り分ける。
3. ACM発行後に新しいTerraform Planを監査し、承認を得てから独自ドメインとCloudflare DNSを有効化し、提出資料を最終同期する。

# 日報 2026-09-07

## 今日行ったこと

- `environment=prod`、`ap-northeast-1`、`activate_custom_domain=false`で本番環境を構築し、state 48エントリ（managed 43、data 5）、再Planの追加0・変更0・削除0を確認した。
- 初回ApplyのOAuth認証失効による中断後、新規Planを監査・承認し、残り42件を適用した。公開サイト、health、demo-reportは200、認証必須reportsの未認証アクセスは401で、DynamoDB、Cognito、Lambda、S3、ACMも期待値どおりとユーザー確認済みである。
- 暗号化ロック付きremote backendは未実装のlocal stateをユーザー承認のまま使用し、stateバックアップを取得した。比較選択カードのWeb資産は公開反映済みである。

## 残っている課題

- ACMはDNS検証待ちであり、CloudFront aliasとCloudflare DNSは未変更である。
- ACM検証完了後の独自ドメイン有効化には、新規Planの監査と承認が必要である。

## 今後の開発工程

1. ACMのDNS検証完了後に新規Planを作成・監査し、承認後に独自ドメインを有効化する。
2. 提出時に公開サイト、health、demo-report、認証必須reportsの確認結果を再確認する。

# 日報 2026-09-11

## 今日行ったこと

- AWS外で削除済みのACMとCognito推定利用者数3→4をrefresh-onlyでstateへ反映し、AWS変更0を確認した。
- Cognito 1件とDynamoDB 4件の削除保護解除Plan（0 add/5 change/0 destroy）を適用し、Destroy Plan（0 add/0 change/42 destroy、SHA256 `E05E92D13FCB1FB0187F376A73ABACF67B5832AF24FB359FBC1ABE4AFDFD4BD8`）をユーザー承認後に適用した。
- 現在の`terraform state list`が0件であること、ローカルstateバックアップがあることを確認した。Cloudflare DNSはTerraform管理外のため残存している。

## 残っている課題

- AWS基盤はDestroy済みであり、再構築が必要な場合は最新stateから新規Planを作成・監査し、明示承認を得る必要がある。
- Cloudflare DNSの残存状態はTerraform管理外であり、AWS再構築や独自ドメイン利用時の扱いは未決である。

## 今後の開発工程

1. AWS基盤を再構築する場合は、新規Planの差分と公開範囲を確認してから実行する。
2. 提出物とローカル成果物の確認範囲を、AWS基盤Destroy済みの状態に合わせて確定する。
